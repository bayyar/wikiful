<!DOCTYPE html>
<html>
<head>
  <title>Wikiful</title>
  <!--[if lt IE 9]> <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script> <![endif]-->
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <meta content="width=device-width, initial-scale=1.0" name="viewport">
</head>
<body>
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Wikiful", root_path, :class => "brand" %>
      <ul class="nav navbar-nav">
      		<li><%= link_to "Articles", articles_path, :class => "active" %></li>
      		<li><%= link_to "Categories", categories_path, :class => "active" %></li>
  	  	</ul>
      <div class="session-controls" style="float: right">
            <% if current_user %>
              <%= link_to "Log Out", logout_path, :class => "btn btn-mini navbar-btn" %>
              Logged in as <%= current_user.email %>
            <% else %>
            <div class="btn-group">
            <%= link_to "Sign Up", signup_path, :class => "btn btn-mini navbar-btn" %>
            <%= link_to "Log In", login_path, :class => "btn btn-mini" %>
            </div>
            <% end %>
        </div>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="span4">
      <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, id:"flash_#{name}" %>
      <% end %>
    </div>
  </div>
<div class="jumbotron">
		<h1>Welcome to Wikiful<br>
			<small>A Thinkful Project</small></h1>
	</div>
<div class ="row" style="padding: 15px">
		<%= yield %>
</div>
</div>
</div>
</body>